# CUSTOM CHANGELOG

本文件只记录本 fork 的定制改动，与上游 `CHANGELOG.md` 完全独立，合并上游时不会产生冲突。

版本号格式：`v<上游基线>-custom.<定制序号>`，例如 `v0.18.0-custom.3` 表示基于上游 v0.18.0 的第 3 次定制发布。
合并新的上游版本后，基线号跟随上游、定制序号从 1 重新开始。

条目格式必须与上游一致（`+ [类型] 内容`），否则前端解析不到。可用类型：新增 / 修复 / 调整 / 优化 / 文档。

## v0.18.0-custom.1 - 2026-09-16

+ [新增] 引入定制版本体系：根目录新增 `CUSTOM_VERSION` 与 `CUSTOM_CHANGELOG.md`，构建时注入前端，与上游 `VERSION` / `CHANGELOG.md` 互不干扰。
+ [新增] 配置页「关于」改为以定制版本为主展示，上游基线与上游最新版本作为副信息，上游有更新时才高亮提示。
+ [新增] 「检查定制更新」按钮，从本仓库 `dev` 分支拉取最新定制版本与定制日志。
+ [新增] 更新日志弹窗拆分为「定制日志」与「上游日志」两个页签，分别标记最新与当前版本。
+ [新增] `scripts/release-custom.mjs` 定制发布脚本，一条命令完成版本号递增、日志骨架、提交与打标签。
